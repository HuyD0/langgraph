# Development environment configuration
# Overrides default.yaml for dev environment

deployment:
  environment: "dev"
  log_level: "DEBUG"
  enable_tracing: true

# Dev-specific model settings
model:
  endpoint_name: "databricks-claude-3-7-sonnet"
  temperature: 0.7
  
  # Example: Enable prompt registry in dev for testing
  # use_prompt_registry: true
  # prompt_name: "agent-system-prompt"
  # prompt_version: "latest"  # or specific version like 1, 2, etc.

# Dev MLflow settings
# The experiment name should match the DAB definition in resources/models/agent_experiment.yml
# DAB will automatically add the "[dev user]" prefix in development mode
mlflow:
  experiment_name: "/Users/huy.d@hotmail.com/langgraph-mcp-agent-dev"
  run_name: "dev_agent_run"
